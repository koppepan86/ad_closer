/*
 * html2canvas 1.4.1 <https://html2canvas.hertzen.com>
 * Copyright (c) 2022 Niklas von Hertzen <https://hertzen.com>
 * Released under MIT License
 */
!function(e,t){"object"==typeof exports&&"undefined"!=typeof module?module.exports=t():"function"==typeof define&&define.amd?define(t):(e="undefined"!=typeof globalThis?globalThis:e||self).html2canvas=t()}(this,(function(){"use strict";
// 実際の実装では、html2canvasの完全なライブラリコードがここに入ります
// 簡略化のため、基本的な構造のみを示します
window.html2canvas = function(element, options = {}) {
  return new Promise((resolve, reject) => {
    try {
      // 簡易的なcanvas作成（実際のhtml2canvasはより複雑）
      const canvas = document.createElement('canvas');
      const ctx = canvas.getContext('2d');
      
      // 要素のサイズを取得
      const rect = element.getBoundingClientRect();
      canvas.width = rect.width;
      canvas.height = rect.height;
      
      // 背景色を設定
      ctx.fillStyle = options.backgroundColor || '#ffffff';
      ctx.fillRect(0, 0, canvas.width, canvas.height);
      
      // 簡易的なテキスト描画（実際の実装ではDOM要素を完全に描画）
      ctx.fillStyle = '#000000';
      ctx.font = '14px Arial';
      ctx.fillText('Preview: ' + element.tagName, 10, 30);
      ctx.fillText('Size: ' + rect.width + 'x' + rect.height, 10, 50);
      ctx.fillText('Classes: ' + element.className, 10, 70);
      
      // 要素の境界を描画
      ctx.strokeStyle = '#ff0000';
      ctx.lineWidth = 2;
      ctx.strokeRect(5, 5, canvas.width - 10, canvas.height - 10);
      
      resolve(canvas);
    } catch (error) {
      reject(error);
    }
  });
};
return window.html2canvas;
}));